from setuptools import setup, find_packages

setup(
    name='CJ_Services', # This is the package name you'll use to import
    version='0.1.0',
    packages=find_packages(), # Automatically finds the 'cj_services' folder
)